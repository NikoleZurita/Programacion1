#include "stdafx.h"
#include <iostream>
#include "conio.h"

using namespace std;

void main()
{
	int N,suma,i,sig;
    cout << "introducir N: ";
	cin>>N;
	suma=0;
	sig=1;
		for(i=1;i<=N;i++)
			suma=suma+sig*i;
		    sig=-1*sig;
	cout << "Suma: ";
	cout  << suma;
	getch();
}
